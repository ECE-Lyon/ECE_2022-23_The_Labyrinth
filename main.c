#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "startGame.h"

int main(void) {
    srand(time(NULL));
    printf("Welcome + explican games's rules");
    //startGame
    return 0;
}
